# 任务列表

- 最近更新：2026-08-09
- 状态：`[ ]` 未开始，`[-]` 进行中，`[x]` 已完成

完成任务时在条目后记录验证证据；只有代码存在但没有对应验证时，不应标记完成。

## 文档与治理

- [x] `TASK-DOC-001` P1 建立需求、架构、契约、质量、运维、计划、治理和 `AGENTS.md` 框架。证据：2026-08-09 文档结构与链接检查。
- [ ] `TASK-DOC-002` P3 为下一次正式发布建立版本号和发布检查单。

## 渲染一致性

- [ ] `TASK-RENDER-001` P1 核对并收敛 Web、MiniProgram、Server 的字符集、色调、亮度映射和模式公式；先建立金样，避免重构改变视觉。
- [ ] `TASK-RENDER-002` P2 决定 Server `glow` 的正式行为并实现或从 API 契约移除，确保小程序控制状态与成片一致。
- [ ] `TASK-RENDER-003` P2 扩充或重新设计 Server 字形渲染，使 DENSE、BLOCK、LINE 在 MP4 中保持可辨差异。
- [ ] `TASK-RENDER-004` P2 回归开屏演示，确认 Web 既定构图和小程序内部缩放互不影响用户媒体。

## 比例与导出

- [ ] `TASK-TEST-001` P1 建立横图、竖图、方图和圆形标记的自动宽高比检查，并覆盖 PNG、WebM、MP4。
- [ ] `TASK-IMG-001` P1 验证并按需修正小程序图片导出，使保存画布严格使用输入显示比例而不是固定 CRT 视口比例。
- [ ] `TASK-VID-003` P2 使用横向、竖向、有声、无声和非 1:1 sample aspect ratio 视频验证 Server 输出。
- [ ] `TASK-VID-004` P3 修正或明确 `metadata.audio`，使其真实表示输出音轨状态。

## 视频处理

- [x] `TASK-VIDEO-001` P2 评估 Web 视频导出替代方案并记录 ADR。证据：ADR-0002，2026-08-09 用户确认推荐方案。
- [x] `TASK-WEB-EXPORT-001` P1 将 Web 迁移到 Vite/npm，使用 Mediabunny + WebCodecs 实现 Worker 离线逐帧导出，MediaRecorder 仅降级。证据：2026-08-09 `npm test`、`npm run build` 和实际 H.264/AAC 视频导出。
- [x] `TASK-WEB-EXPORT-002` P1 实现快速/高清/精细和源/24/30/60 FPS 设置、预计信息、进度、帧数和取消。证据：2026-08-09 横竖视频、30→24 FPS、取消和两档视口验证。
- [-] `TASK-WEB-EXPORT-003` P1 验证 H.264/MP4、VP9/WebM、音频直通/转码/无声告警和预览状态恢复。H.264/MP4、AAC 直通、三模式与状态恢复已通过；VP9、音频转码和静音告警仍缺目标环境动态验证。
- [ ] `TASK-VIDEO-002` P2 为小程序视频任务增加阶段状态或进度能力，避免长请求只有笼统等待。
- [ ] `TASK-VIDEO-003` P2 对上传、解码、Node 渲染、编码、复用、下载分别计时，再确定性能优化目标。

## 配置与运维

- [ ] `TASK-CONFIG-001` P1 将小程序 API 基址从页面硬编码迁移到单一环境配置入口，支持模拟器、局域网真机和生产 HTTPS。
- [ ] `TASK-OPS-001` P1 为 `storage/uploads`、`temp`、`outputs` 设计保留时间和安全清理任务。
- [ ] `TASK-OPS-002` P2 改善 Server 错误分类，将上传超限、媒体无效和 FFmpeg 失败返回为可操作错误。
- [ ] `TASK-OPS-003` P2 正式部署前加入文件类型检查、限流、超时、鉴权/额度和监控。

## 兼容性

- [ ] `TASK-COMPAT-001` P1 完成一台 iOS 微信真机的图片、三模式、视频播放和保存回归。
- [ ] `TASK-COMPAT-002` P2 记录 Android/微信/基础库/开发者工具的准确版本，补全兼容性矩阵。
- [ ] `TASK-COMPAT-003` P2 检查项目源文件 UTF-8 和中文文案显示，防止工具链或 shell 编码造成乱码误判。
